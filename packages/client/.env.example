# Optional: full origin of the API (no trailing slash). If unset, use Vite dev proxy to same-origin `/api/v1/...`.
# VITE_API_URL=http://localhost:3000

# Set to "true" to use in-memory mocks for menu/category/item data (CSV and scan steps stay mock regardless).
# VITE_USE_MOCK_API=true
